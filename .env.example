# --- Local dev (npm start) ---
DB_HOST=localhost
DB_PORT=5434

# --- EC2 / Docker Compose (backend container) ---
# DB_HOST=postgres-db
# DB_PORT=5432
# (docker-compose.yml overrides DB_HOST for backend-app automatically)

DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=stockwiz
PORT=3000
